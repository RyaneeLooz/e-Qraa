{
  "name": "e-qraa",
  "version": "1.0.0",
  "description": "e-Qraa educational marketplace — monorepo root runner",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start:backend": "npm run start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "test": "node scripts/health-test.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
